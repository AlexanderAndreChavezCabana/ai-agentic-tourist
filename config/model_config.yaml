# Configuración de Modelos LLM
models:
  openai:
    provider: "openai"
    model_name: "gpt-4o"
    temperature: 0.7
    max_tokens: 2048

# Modelo por defecto
default_model: "openai"

# Timeout para llamadas a API
api_timeout: 30

# Reintentos
max_retries: 3
retry_delay: 1
