# Configuración de Modelos LLM
models:
  google:
    provider: "google"
    model_name: "gemini-pro"
    temperature: 0.7
    max_tokens: 2048
    top_p: 0.9

# Modelo por defecto
default_model: "google"

# Timeout para llamadas a API
api_timeout: 30

# Reintentos
max_retries: 3
retry_delay: 1
